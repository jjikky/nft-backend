const V1 = "/v1";
const V2 = "/v2";
const HOME = "/";
const MINTING = "/minting";

// const NAME = "/name/:name";

const routes = {
  v1: V1,
  v2: V2,
  home: HOME,
  minting: MINTING,
};

module.exports = routes;
