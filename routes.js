const V1 = "/api/v1";
const HOME = "/";
const MINTING = "/minting";
const NFTS = "/nfts";
const TEST = "/test";

// const NAME = "/name/:name";

const routes = {
  v1: V1,
  home: HOME,
  minting: MINTING,
  test: TEST,
  nfts: NFTS,
};

module.exports = routes;
